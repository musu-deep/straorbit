# Optional. Without a key, STRAORBIT runs with the built-in simulation mode.
GEMINI_API_KEY=

# Stable production model used by the strategic copilot.
GEMINI_MODEL=gemini-3.5-flash

# Hosting providers usually inject PORT automatically.
PORT=3000
NODE_ENV=development
